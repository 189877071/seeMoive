<view class="nav-box shadow black">
  <view 
    wx:for="{{navs}}" 
    wx:key="{{index}}" 
    id="{{item.id}}" 
    class="nav-item {{index === navSelected ? 'selected' : ''}}"
    >
    {{item.name}}
  </view>
</view>

<scroll-view scroll-y class='scroll'>
  <view style='height: 30rpx;'></view>
  <home-card wx:for="{{3}}" wx:key="{{index}}"></home-card>
</scroll-view>
<my-login wx:if="{{login}}"></my-login>
